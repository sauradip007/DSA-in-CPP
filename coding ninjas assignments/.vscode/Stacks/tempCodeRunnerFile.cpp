
        s.pop();
        count++;
    }
    
    }

    if(count > 0)
    {
        return true;
    }
    return false;

    // while(e[i] != '\0')
    // {
    //     if(e[i] == '(' || e[i] == '{' || e[i] == '[')